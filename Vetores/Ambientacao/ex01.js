let readlineSync = require('readline-sync');
let N = parseFloat(readlineSync.question("Digite um numero N: ")); //quantidade de Comandos

let lista = []; //definindo a array

for (let i = 1; i <= N; i++){
    comando = input()
}